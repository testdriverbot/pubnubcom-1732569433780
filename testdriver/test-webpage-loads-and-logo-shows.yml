version: 4.1.27
steps:
  - prompt: Navigate to the homepage, if not already there.
    commands:
      - command: press-keys
        keys:
          - f5
  - prompt: >-
      Assert that the logo "PubNub" is visible on the page.  # Use the content
      of the file from the environment
    commands:
      - command: assert
        expect: the "PubNub" logo is visible on the page
